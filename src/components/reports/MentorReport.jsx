import { CheckCircle2, Sparkles } from "lucide-react";

const text = (value, fallback = "Not provided.") => typeof value === "string" && value.trim() ? value : fallback;
function ListCard({ title, items }) { const values = Array.isArray(items) ? items.filter(Boolean) : []; return <article className="min-w-0 rounded-2xl border border-white/10 bg-[#0F172A] p-4 sm:p-5"><h3 className="text-sm font-semibold uppercase tracking-[0.14em] text-violet-200">{title}</h3>{values.length ? <ul className="mt-3 space-y-3">{values.map((item, index) => <li key={`${item}-${index}`} className="flex min-w-0 gap-3 break-words text-sm leading-7 text-slate-300"><CheckCircle2 className="mt-1 size-4 shrink-0 text-emerald-300" />{text(item)}</li>)}</ul> : <p className="mt-3 text-sm leading-7 text-slate-400">No specific guidance applies here.</p>}</article>; }
export default function MentorReport({ answer = {} }) { return <section aria-labelledby="mentor-report-heading" className="min-w-0 rounded-[28px] border border-white/10 bg-[#121b2e] p-5 shadow-[0_16px_48px_rgba(0,0,0,0.2)] sm:p-6"><div><span className="inline-flex items-center gap-2 rounded-full bg-violet-400/15 px-3 py-1 text-xs font-semibold text-violet-100 ring-1 ring-violet-400/25"><Sparkles className="size-3.5" />AI Analysis</span><h2 id="mentor-report-heading" className="mt-3 break-words text-xl font-semibold text-white">{text(answer.topic, "Mentor guidance")}</h2><p className="mt-2 break-words text-sm leading-7 text-slate-300">{text(answer.summary)}</p></div><div className="mt-6 grid min-w-0 gap-4 md:grid-cols-2"><article className="min-w-0 rounded-2xl border border-white/10 bg-[#0F172A] p-4 sm:p-5 md:col-span-2"><h3 className="text-sm font-semibold uppercase tracking-[0.14em] text-violet-200">Why it matters</h3><p className="mt-3 break-words text-sm leading-7 text-slate-300">{text(answer.whyItMatters)}</p></article><ListCard title="What is wrong" items={answer.whatIsWrong} /><ListCard title="Best practices" items={answer.bestPractices} /><ListCard title="Better approach" items={answer.betterApproach} /><article className="min-w-0 rounded-2xl border border-white/10 bg-[#0F172A] p-4 sm:p-5"><h3 className="text-sm font-semibold uppercase tracking-[0.14em] text-violet-200">Example code</h3><pre className="mt-3 max-w-full overflow-x-auto rounded-xl bg-slate-950/80 p-4 text-xs leading-6 text-cyan-100 sm:text-sm"><code>{text(answer.exampleCode, "No example code was returned.")}</code></pre></article><ListCard title="Learning tips" items={answer.learningTips} /><ListCard title="Next steps" items={answer.nextSteps} /></div></section>; }
