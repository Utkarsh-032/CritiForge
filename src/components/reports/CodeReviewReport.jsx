import { CheckCircle2, ChevronDown, Sparkles } from "lucide-react";
import { useState } from "react";
import ScoreCard from "./ScoreCard";
import ScoreProgress from "./ScoreProgress";
const severityStyles = { critical: "bg-rose-500/20 text-rose-100 ring-rose-400/30", high: "bg-rose-400/15 text-rose-200 ring-rose-400/25", medium: "bg-amber-400/15 text-amber-200 ring-amber-400/25", low: "bg-cyan-400/15 text-cyan-200 ring-cyan-400/25" }; const scoreLabels = { quality: "Quality", maintainability: "Maintainability", security: "Security", performance: "Performance", bestPractices: "Best Practices" }; const text = (value, fallback = "Not provided.") => typeof value === "string" && value.trim() ? value : fallback;
function IssueCard({ issue = {} }) { const [expanded, setExpanded] = useState(false); const severity = severityStyles[issue.severity] ? issue.severity : "low"; return <article className="min-w-0 rounded-2xl border border-white/10 bg-[#0F172A] p-4"><button type="button" className="flex w-full items-start justify-between gap-3 text-left" onClick={() => setExpanded((value) => !value)} aria-expanded={expanded}><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h3 className="break-words font-semibold text-white">{text(issue.title, "Review finding")}</h3><span className="rounded-full bg-white/5 px-2 py-0.5 text-xs text-slate-300">{text(issue.category, "general")}</span></div><p className="mt-1 break-words text-xs text-slate-500">{text(issue.lineReference, "Location not specified")}</p></div><span className={`flex shrink-0 items-center gap-1 rounded-full px-2.5 py-1 text-xs font-semibold capitalize ring-1 ${severityStyles[severity]}`}><ChevronDown className={`size-3.5 transition-transform duration-150 ${expanded ? "rotate-180" : ""}`} />{severity}</span></button>{expanded && <div className="mt-4 space-y-3 border-t border-white/10 pt-4 text-sm leading-6"><p className="break-words text-slate-300">{text(issue.explanation)}</p><p className="break-words text-slate-300"><span className="font-medium text-violet-200">Suggested fix: </span>{text(issue.suggestedFix)}</p></div>}</article>; }
export default function CodeReviewReport({ report = {} }) { const issues = Array.isArray(report.issues) ? report.issues : []; const strengths = Array.isArray(report.strengths) ? report.strengths : []; const suggestions = Array.isArray(report.refactoringSuggestions) ? report.refactoringSuggestions : []; const scores = report.scores && typeof report.scores === "object" ? report.scores : {}; const counts = ["critical", "high", "medium", "low"].map((severity) => ({ severity, count: issues.filter((issue) => issue?.severity === severity).length })); return <section className="min-w-0 space-y-6 rounded-[28px] border border-white/10 bg-[#121b2e] p-5 shadow-[0_16px_48px_rgba(0,0,0,0.2)] sm:p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div className="min-w-0 max-w-3xl"><span className="inline-flex items-center gap-2 rounded-full bg-violet-400/15 px-3 py-1 text-xs font-semibold text-violet-100 ring-1 ring-violet-400/25"><Sparkles className="size-3.5" />AI Analysis</span><h2 className="mt-3 text-lg font-semibold text-white">Code review report</h2><p className="mt-1 break-words text-sm leading-6 text-slate-300">{text(report.summary)}</p></div><ScoreCard label="Overall score" score={report.overallScore} /></div><div className="rounded-2xl border border-white/10 bg-[#0F172A] p-4 sm:p-5"><h2 className="font-semibold text-white">Score breakdown</h2><div className="mt-5 space-y-4">{Object.entries(scoreLabels).map(([key, label]) => <ScoreProgress key={key} label={label} score={scores[key]} />)}</div></div><div><div className="flex flex-wrap items-center justify-between gap-3"><h2 className="text-lg font-semibold text-white">Review findings</h2><div className="flex flex-wrap gap-2">{counts.map(({ severity, count }) => <span key={severity} className={`rounded-full px-2.5 py-1 text-xs font-semibold capitalize ring-1 ${severityStyles[severity]}`}>{count} {severity}</span>)}</div></div>{issues.length ? <div className="mt-4 grid min-w-0 gap-3 md:grid-cols-2">{issues.map((issue, index) => <IssueCard key={`${issue?.title || "issue"}-${index}`} issue={issue} />)}</div> : <p className="mt-4 rounded-xl border border-white/10 bg-[#0F172A] p-4 text-sm text-slate-400">No meaningful issues were identified.</p>}</div><div><h2 className="text-lg font-semibold text-white">Strengths</h2>{strengths.length ? <ul className="mt-4 grid gap-3 md:grid-cols-2">{strengths.map((strength, index) => <li key={`${strength}-${index}`} className="min-w-0 break-words rounded-xl border border-white/5 bg-white/[0.03] p-3 text-sm leading-6 text-slate-300"><CheckCircle2 className="mr-3 inline size-5 text-emerald-300" />{text(strength)}</li>)}</ul> : <p className="mt-3 text-sm text-slate-400">No strengths were returned.</p>}</div><div><h2 className="text-lg font-semibold text-white">Refactoring suggestions</h2>{suggestions.length ? <div className="mt-4 grid gap-3">{suggestions.map((suggestion, index) => <article key={`${suggestion?.title || "suggestion"}-${index}`} className="min-w-0 rounded-2xl border border-white/10 bg-[#0F172A] p-4"><h3 className="break-words font-semibold text-white">{text(suggestion?.title, "Suggestion")}</h3><p className="mt-2 break-words text-sm leading-6 text-slate-300">{text(suggestion?.reason)}</p>{suggestion?.exampleCode ? <pre className="mt-4 max-w-full overflow-x-auto rounded-xl border border-white/10 bg-slate-950/80 p-4 text-xs leading-6 text-cyan-100 sm:text-sm"><code>{suggestion.exampleCode}</code></pre> : null}</article>)}</div> : <p className="mt-3 text-sm text-slate-400">No refactoring suggestions were returned.</p>}</div></section>; }
